server:
  port: 8082

spring:
  application:
    name: customer
  datasource:
    username: 'ErdiSyla'
    url: jdbc:postgresql://localhost:5500/customer
    password: 'lightweightNoReaction123'
  jpa:
    properties:
      hibernate:
        format_sql: 'true'
    hibernate:
      ddl-auto: update
    show-sql: 'true'

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka    